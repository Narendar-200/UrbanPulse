# UrbanPulse Backend

Express API for the UrbanPulse frontend.

## Setup

1. Copy `.env.example` to `.env`.
2. Set `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`.
3. Install and run:

```bash
npm install
npm run dev
```

Server runs on `http://localhost:4000` by default.
